from setuptools import setup

setup(
   name='python_code_visualizer',
   version='1.0',
   description='Parses a code base and generates a visualization of the parsed code',
   author='stein-k',
   author_email='karlsen.stein@gmail.com',
   packages=['python_code_visualizer'],
   install_requires=['pytest'],
)
